name: Test Org Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Sanity
        run: |
          echo "Org runner is live 🚀"
          hostname
          uname -a
